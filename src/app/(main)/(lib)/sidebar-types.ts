export interface IChat {
  roomId: string;
  topicTitle: string;
  lastMessage: string;
  lastMessageTime: string;
}
export interface INotification {
  id: string;
  title: string;
  description: string;
  time: string;
}
